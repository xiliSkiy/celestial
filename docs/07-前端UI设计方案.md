# Gravital Core - 前端 UI 设计方案

## 1. 设计理念

### 1.1 核心原则
- **简洁高效**: 专注于监控核心功能，避免过度设计
- **数据可视化**: 突出关键指标和趋势图表
- **响应式设计**: 支持桌面端和移动端访问
- **暗色主题**: 适合长时间监控场景，减少视觉疲劳
- **实时更新**: WebSocket 实时推送数据变化

### 1.2 技术栈推荐

#### 前端框架
- **Vue 3** + **TypeScript** + **Vite**
  - 轻量级、性能优异
  - 组合式 API，代码组织清晰
  - TypeScript 类型安全

#### UI 组件库
- **Element Plus** 或 **Ant Design Vue**
  - 成熟的企业级组件库
  - 丰富的组件生态
  - 良好的文档和社区支持

#### 图表库
- **Apache ECharts**
  - 强大的数据可视化能力
  - 丰富的图表类型
  - 良好的性能

#### 状态管理
- **Pinia**
  - Vue 3 官方推荐
  - 简洁的 API
  - TypeScript 支持

#### 其他工具
- **Axios**: HTTP 请求
- **Socket.io**: WebSocket 实时通信
- **Day.js**: 时间处理
- **VueUse**: 组合式工具库

## 2. 整体布局

### 2.1 主布局结构

```
┌─────────────────────────────────────────────────────────────┐
│  Header (顶部导航栏)                                          │
│  Logo | 系统名称 | 搜索框 | 通知 | 用户信息                    │
├──────────┬──────────────────────────────────────────────────┤
│          │                                                  │
│  Sidebar │           Main Content Area                      │
│  (侧边栏) │           (主内容区)                              │
│          │                                                  │
│  - 仪表盘 │  ┌────────────────────────────────────────┐     │
│  - 设备   │  │                                        │     │
│  - Sentinel│  │         页面内容                        │     │
│  - 任务   │  │                                        │     │
│  - 告警   │  │                                        │     │
│  - 转发器 │  │                                        │     │
│  - 系统   │  └────────────────────────────────────────┘     │
│          │                                                  │
└──────────┴──────────────────────────────────────────────────┘
```

### 2.2 配色方案

#### 暗色主题（默认）
```css
--bg-primary: #1a1a1a;        /* 主背景 */
--bg-secondary: #2d2d2d;      /* 次级背景 */
--bg-tertiary: #3d3d3d;       /* 卡片背景 */
--text-primary: #ffffff;       /* 主文本 */
--text-secondary: #b3b3b3;     /* 次级文本 */
--border-color: #404040;       /* 边框颜色 */

/* 功能色 */
--color-primary: #409eff;      /* 主题色 */
--color-success: #67c23a;      /* 成功 */
--color-warning: #e6a23c;      /* 警告 */
--color-danger: #f56c6c;       /* 危险 */
--color-info: #909399;         /* 信息 */
```

#### 亮色主题
```css
--bg-primary: #ffffff;
--bg-secondary: #f5f7fa;
--bg-tertiary: #ffffff;
--text-primary: #303133;
--text-secondary: #606266;
--border-color: #dcdfe6;
```

## 3. 页面设计

### 3.1 仪表盘 (Dashboard)

#### 3.1.1 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│  统计卡片区                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │ 设备总数  │ │ 在线设备  │ │ 活跃告警  │ │ 任务数   │        │
│  │  1,234   │ │  1,180   │ │    45    │ │  3,456  │        │
│  │  ↑ 5.2%  │ │  ↑ 2.1%  │ │  ↓ 12%   │ │  ↑ 8.3% │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
├─────────────────────────────────────────────────────────────┤
│  图表区                                                        │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  设备状态分布           │ │  告警趋势图              │       │
│  │  (饼图)                │ │  (折线图)               │       │
│  │                        │ │                        │       │
│  └────────────────────────┘ └────────────────────────┘       │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  Sentinel 状态          │ │  数据转发统计            │       │
│  │  (列表)                │ │  (柱状图)               │       │
│  │                        │ │                        │       │
│  └────────────────────────┘ └────────────────────────┘       │
├─────────────────────────────────────────────────────────────┤
│  最近活动                                                      │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ • 设备 server-001 状态变更为离线          2分钟前     │     │
│  │ • 新增告警规则 "CPU 使用率过高"            5分钟前     │     │
│  │ • Sentinel sentinel-office-1 心跳超时     10分钟前    │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.1.2 核心组件
- **StatCard**: 统计卡片
  - 显示数值、标题、趋势
  - 支持点击跳转详情
  
- **ChartCard**: 图表卡片
  - ECharts 图表容器
  - 支持刷新、导出

- **ActivityTimeline**: 活动时间线
  - 实时更新
  - 分类显示（设备、告警、任务等）

### 3.2 设备管理

#### 3.2.1 设备列表页
```
┌─────────────────────────────────────────────────────────────┐
│  操作栏                                                        │
│  [+ 添加设备] [批量导入] [导出] | 搜索: [________] [筛选▼]    │
├─────────────────────────────────────────────────────────────┤
│  设备分组树 (左侧)        │  设备列表 (右侧)                   │
│  ┌──────────────┐        │  ┌──────────────────────────┐    │
│  │ □ 全部设备    │        │  │ ID | 名称 | 类型 | 状态   │    │
│  │ ▼ 生产环境    │        │  ├──────────────────────────┤    │
│  │   ▶ 北京机房  │        │  │ 001 | web-1 | 服务器 | 🟢│    │
│  │   ▶ 上海机房  │        │  │ 002 | db-1  | 数据库 | 🟢│    │
│  │ ▼ 测试环境    │        │  │ 003 | sw-1  | 交换机 | 🔴│    │
│  │   ▶ 测试机房  │        │  └──────────────────────────┘    │
│  └──────────────┘        │  [分页: 1 2 3 ... 10]             │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.2 设备详情页
```
┌─────────────────────────────────────────────────────────────┐
│  设备信息                                                      │
│  名称: web-server-001          类型: Linux 服务器             │
│  状态: 🟢 在线                 分组: 生产环境/北京机房          │
│  标签: [prod] [critical] [web]                               │
│  [编辑] [删除] [测试连接]                                      │
├─────────────────────────────────────────────────────────────┤
│  Tab 导航: [概览] [监控指标] [采集任务] [告警规则] [历史记录]  │
├─────────────────────────────────────────────────────────────┤
│  概览 Tab                                                     │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  基本信息               │ │  连接配置               │       │
│  │  设备ID: dev-001        │ │  IP: 192.168.1.100     │       │
│  │  Sentinel: sentinel-1   │ │  端口: 22              │       │
│  │  创建时间: 2025-01-01   │ │  协议: SSH             │       │
│  └────────────────────────┘ └────────────────────────┘       │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  实时监控指标                                        │     │
│  │  CPU: [========>  ] 80%    内存: [=====>    ] 50%   │     │
│  │  磁盘: [===>      ] 30%    网络: 125 MB/s           │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.2.3 添加/编辑设备对话框
```
┌─────────────────────────────────────┐
│  添加设备                     [×]    │
├─────────────────────────────────────┤
│  基本信息                            │
│  设备名称: [________________]        │
│  设备类型: [服务器 ▼]                │
│  所属分组: [生产环境/北京机房 ▼]     │
│  标签: [prod] [×] [+ 添加]          │
│                                     │
│  连接配置                            │
│  IP地址: [________________]          │
│  端口: [22___]                       │
│  协议: [SSH ▼]                       │
│  用户名: [________________]          │
│  密码: [________________]            │
│                                     │
│  分配 Sentinel                       │
│  □ 自动分配                          │
│  ○ 手动指定: [sentinel-1 ▼]         │
│                                     │
│         [取消]  [测试连接]  [保存]   │
└─────────────────────────────────────┘
```

### 3.3 Sentinel 管理

#### 3.3.1 Sentinel 列表页
```
┌─────────────────────────────────────────────────────────────┐
│  操作栏                                                        │
│  [+ 注册 Sentinel] | 搜索: [________] 状态: [全部 ▼]          │
├─────────────────────────────────────────────────────────────┤
│  Sentinel 卡片网格                                            │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────┐ │
│  │ sentinel-office-1│ │ sentinel-office-2│ │ sentinel-dc-1│ │
│  │ 🟢 在线           │ │ 🟢 在线           │ │ 🔴 离线      │ │
│  │ 北京办公室        │ │ 上海办公室        │ │ 数据中心     │ │
│  │ ─────────────── │ │ ─────────────── │ │ ────────────│ │
│  │ 设备: 45         │ │ 设备: 32         │ │ 设备: 128    │ │
│  │ 任务: 120        │ │ 任务: 89         │ │ 任务: 456    │ │
│  │ CPU: 25%         │ │ CPU: 18%         │ │ CPU: --      │ │
│  │ 内存: 512MB      │ │ 内存: 384MB      │ │ 内存: --     │ │
│  │ 版本: v1.0.0     │ │ 版本: v1.0.0     │ │ 版本: v0.9.0 │ │
│  │ [详情] [控制]    │ │ [详情] [控制]    │ │ [详情] [删除]│ │
│  └──────────────────┘ └──────────────────┘ └──────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 3.3.2 Sentinel 详情页
```
┌─────────────────────────────────────────────────────────────┐
│  Sentinel 信息                                                │
│  ID: sentinel-office-1          名称: 北京办公室 Sentinel     │
│  状态: 🟢 在线 (最后心跳: 5秒前)  版本: v1.0.0                 │
│  [重启] [更新配置] [删除]                                      │
├─────────────────────────────────────────────────────────────┤
│  Tab 导航: [概览] [设备列表] [任务列表] [性能监控] [日志]     │
├─────────────────────────────────────────────────────────────┤
│  概览 Tab                                                     │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  系统信息               │ │  网络信息               │       │
│  │  主机名: office-gw-1    │ │  IP: 192.168.1.10      │       │
│  │  OS: Linux Ubuntu 22.04 │ │  区域: office-beijing  │       │
│  │  架构: x86_64           │ │  注册时间: 2025-01-01  │       │
│  └────────────────────────┘ └────────────────────────┘       │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  性能指标 (实时)                                     │     │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐│     │
│  │  │ CPU 使用率   │ │ 内存使用     │ │ 磁盘使用     ││     │
│  │  │   25%        │ │   512MB      │ │   2.1GB      ││     │
│  │  └──────────────┘ └──────────────┘ └──────────────┘│     │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐│     │
│  │  │ 管理设备数   │ │ 活跃任务     │ │ 运行时间     ││     │
│  │  │   45         │ │   120        │ │   15天       ││     │
│  │  └──────────────┘ └──────────────┘ └──────────────┘│     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  心跳历史 (折线图)                                   │     │
│  │  显示最近 24 小时的心跳延迟                          │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 3.4 任务管理

#### 3.4.1 任务列表页
```
┌─────────────────────────────────────────────────────────────┐
│  操作栏                                                        │
│  [+ 创建任务] | 搜索: [________] 状态: [全部 ▼] 类型: [全部▼]│
├─────────────────────────────────────────────────────────────┤
│  任务列表                                                      │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ ID | 任务名称 | 设备 | Sentinel | 插件 | 间隔 | 状态 │     │
│  ├─────────────────────────────────────────────────────┤     │
│  │ 001 | Ping web-1 | web-1 | sentinel-1 | ping | 60s|🟢│     │
│  │ 002 | SNMP sw-1  | sw-1  | sentinel-1 | snmp | 30s|🟢│     │
│  │ 003 | SSH db-1   | db-1  | sentinel-2 | ssh  | 60s|🔴│     │
│  │ 004 | HTTP api-1 | api-1 | sentinel-1 | http | 10s|🟢│     │
│  └─────────────────────────────────────────────────────┘     │
│  [分页: 1 2 3 ... 20]                                         │
├─────────────────────────────────────────────────────────────┤
│  任务执行统计                                                  │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  成功率趋势图           │ │  执行时间分布           │       │
│  │  (折线图)              │ │  (柱状图)              │       │
│  └────────────────────────┘ └────────────────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

#### 3.4.2 创建/编辑任务对话框
```
┌─────────────────────────────────────┐
│  创建采集任务                 [×]    │
├─────────────────────────────────────┤
│  基本信息                            │
│  任务名称: [________________]        │
│  目标设备: [web-server-001 ▼]       │
│  采集插件: [Ping ▼]                  │
│                                     │
│  调度配置                            │
│  采集间隔: [60__] 秒                 │
│  超时时间: [30__] 秒                 │
│  重试次数: [3___]                    │
│  优先级: [5___] (1-10)               │
│                                     │
│  分配策略                            │
│  ○ 自动分配 (根据负载)               │
│  ○ 手动指定: [sentinel-1 ▼]         │
│                                     │
│  插件配置 (JSON)                     │
│  ┌─────────────────────────────┐   │
│  │ {                           │   │
│  │   "host": "192.168.1.100",  │   │
│  │   "count": 4,               │   │
│  │   "timeout": 5              │   │
│  │ }                           │   │
│  └─────────────────────────────┘   │
│                                     │
│         [取消]  [测试]  [保存]      │
└─────────────────────────────────────┘
```

### 3.5 告警管理

#### 3.5.1 告警规则列表
```
┌─────────────────────────────────────────────────────────────┐
│  Tab 导航: [告警规则] [告警事件] [告警统计]                   │
├─────────────────────────────────────────────────────────────┤
│  操作栏                                                        │
│  [+ 创建规则] | 搜索: [________] 级别: [全部 ▼] 状态: [启用▼]│
├─────────────────────────────────────────────────────────────┤
│  规则列表                                                      │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ 规则名称 | 级别 | 条件 | 通知方式 | 状态 | 操作      │     │
│  ├─────────────────────────────────────────────────────┤     │
│  │ CPU 使用率过高 | ⚠️ | cpu>80% | 邮件,钉钉 | 🟢 | [编辑]│     │
│  │ 内存不足 | 🔴 | mem<10% | 邮件,短信 | 🟢 | [编辑]     │     │
│  │ 磁盘空间告警 | ⚠️ | disk>90% | 邮件 | 🔴 | [编辑]     │     │
│  │ 网络延迟高 | 💡 | latency>100ms | 邮件 | 🟢 | [编辑]  │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.5.2 告警事件列表
```
┌─────────────────────────────────────────────────────────────┐
│  筛选栏                                                        │
│  时间: [最近24小时 ▼] 级别: [全部 ▼] 状态: [全部 ▼]          │
├─────────────────────────────────────────────────────────────┤
│  告警事件时间线                                                │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ 🔴 [Critical] CPU 使用率过高                         │     │
│  │    设备: web-server-001 | 当前值: 95%               │     │
│  │    触发时间: 2025-11-02 12:30:15                    │     │
│  │    持续时间: 15分钟                                  │     │
│  │    [确认] [解决] [静默1小时]                         │     │
│  ├─────────────────────────────────────────────────────┤     │
│  │ ⚠️ [Warning] 内存使用率偏高                          │     │
│  │    设备: db-server-002 | 当前值: 85%                │     │
│  │    触发时间: 2025-11-02 12:25:30                    │     │
│  │    持续时间: 20分钟                                  │     │
│  │    [确认] [解决] [静默1小时]                         │     │
│  ├─────────────────────────────────────────────────────┤     │
│  │ ✅ [Resolved] 网络延迟恢复正常                       │     │
│  │    设备: gateway-001 | 解决时间: 2025-11-02 12:20  │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.5.3 创建告警规则对话框
```
┌─────────────────────────────────────┐
│  创建告警规则                 [×]    │
├─────────────────────────────────────┤
│  基本信息                            │
│  规则名称: [________________]        │
│  告警级别: [Warning ▼]               │
│  描述: [____________________]        │
│                                     │
│  触发条件                            │
│  指标: [cpu_usage ▼]                 │
│  操作符: [> ▼]                       │
│  阈值: [80___] %                     │
│  持续时间: [5___] 分钟               │
│                                     │
│  筛选条件                            │
│  设备类型: [服务器 ▼]                │
│  设备分组: [生产环境 ▼]              │
│  标签: [prod] [×] [+ 添加]          │
│                                     │
│  通知配置                            │
│  ☑ 邮件: [ops@company.com]          │
│  ☑ 钉钉: [选择群组 ▼]                │
│  ☐ 短信: [手机号]                    │
│  ☐ Webhook: [URL]                   │
│                                     │
│  高级设置                            │
│  静默时间: [周末 00:00-23:59]        │
│  抑制规则: [设备离线时不发送 ▼]      │
│                                     │
│         [取消]  [测试]  [保存]      │
└─────────────────────────────────────┘
```

### 3.6 数据转发管理

#### 3.6.1 转发器列表
```
┌─────────────────────────────────────────────────────────────┐
│  操作栏                                                        │
│  [+ 添加转发器] [重新加载配置] | 搜索: [________]              │
├─────────────────────────────────────────────────────────────┤
│  转发器卡片                                                    │
│  ┌──────────────────┐ ┌──────────────────┐ ┌──────────────┐ │
│  │ VictoriaMetrics  │ │ ClickHouse       │ │ Prometheus   │ │
│  │ 🟢 运行中         │ │ 🟢 运行中         │ │ 🔴 已禁用    │ │
│  │ ─────────────── │ │ ─────────────── │ │ ────────────│ │
│  │ 类型: victoria-  │ │ 类型: clickhouse │ │ 类型: prom   │ │
│  │       metrics    │ │                  │ │              │ │
│  │ 端点: :8428      │ │ DSN: :9000       │ │ 端点: :9090  │ │
│  │ ─────────────── │ │ ─────────────── │ │ ────────────│ │
│  │ 成功: 125,430    │ │ 成功: 98,234     │ │ 成功: 0      │ │
│  │ 失败: 12         │ │ 失败: 5          │ │ 失败: 0      │ │
│  │ 延迟: 15ms       │ │ 延迟: 28ms       │ │ 延迟: --     │ │
│  │ [详情] [编辑]    │ │ [详情] [编辑]    │ │ [详情] [启用]│ │
│  └──────────────────┘ └──────────────────┘ └──────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  缓冲区状态                                                    │
│  使用: 1,234 / 10,000 (12.3%)  [============>           ]    │
│  刷新间隔: 10秒 | 批次大小: 1000                              │
└─────────────────────────────────────────────────────────────┘
```

#### 3.6.2 转发器详情页
```
┌─────────────────────────────────────────────────────────────┐
│  转发器信息                                                    │
│  名称: victoria-prod            类型: VictoriaMetrics         │
│  状态: 🟢 运行中                 端点: http://victoria:8428    │
│  [编辑] [禁用] [删除]                                          │
├─────────────────────────────────────────────────────────────┤
│  Tab 导航: [概览] [统计信息] [配置] [日志]                    │
├─────────────────────────────────────────────────────────────┤
│  统计信息 Tab                                                 │
│  ┌────────────────────────┐ ┌────────────────────────┐       │
│  │  成功率                 │ │  吞吐量                 │       │
│  │  99.99%                │ │  5,234 metrics/s       │       │
│  └────────────────────────┘ └────────────────────────┘       │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  成功/失败趋势 (折线图)                              │     │
│  │  显示最近 24 小时的转发成功率和失败次数              │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  延迟分布 (柱状图)                                   │     │
│  │  显示不同延迟区间的请求分布                          │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  历史统计记录                                        │     │
│  │  时间 | 成功数 | 失败数 | 总字节数 | 平均延迟        │     │
│  │  12:00 | 5,234 | 2 | 1.2MB | 15ms                   │     │
│  │  11:00 | 4,987 | 1 | 1.1MB | 14ms                   │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 3.7 系统设置

#### 3.7.1 系统配置页
```
┌─────────────────────────────────────────────────────────────┐
│  Tab 导航: [基本设置] [用户管理] [角色权限] [系统日志]        │
├─────────────────────────────────────────────────────────────┤
│  基本设置 Tab                                                 │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  系统信息                                            │     │
│  │  版本: v1.0.0                                        │     │
│  │  构建时间: 2025-11-02 12:00:00                      │     │
│  │  运行时间: 15天 3小时 25分钟                         │     │
│  │  Go 版本: go1.21                                     │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  数据库配置                                          │     │
│  │  PostgreSQL: 🟢 已连接                              │     │
│  │  Redis: 🟢 已连接                                   │     │
│  │  [测试连接]                                          │     │
│  └─────────────────────────────────────────────────────┘     │
│  ┌─────────────────────────────────────────────────────┐     │
│  │  系统限制                                            │     │
│  │  最大设备数: [10000__]                               │     │
│  │  最大 Sentinel 数: [1000__]                          │     │
│  │  每个 Sentinel 最大任务数: [100__]                   │     │
│  │  数据保留天数: [90___]                               │     │
│  │  [保存]                                              │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.7.2 用户管理页
```
┌─────────────────────────────────────────────────────────────┐
│  操作栏                                                        │
│  [+ 添加用户] | 搜索: [________] 角色: [全部 ▼]               │
├─────────────────────────────────────────────────────────────┤
│  用户列表                                                      │
│  ┌─────────────────────────────────────────────────────┐     │
│  │ 用户名 | 邮箱 | 角色 | 状态 | 最后登录 | 操作        │     │
│  ├─────────────────────────────────────────────────────┤     │
│  │ admin | admin@company.com | 管理员 | 🟢 | 5分钟前 |[编辑]│     │
│  │ operator | ops@company.com | 运维 | 🟢 | 1小时前 |[编辑]│     │
│  │ viewer | view@company.com | 只读 | 🔴 | 3天前 |[编辑]  │     │
│  └─────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

## 4. 组件设计

### 4.1 通用组件

#### 4.1.1 StatCard (统计卡片)
```vue
<template>
  <div class="stat-card">
    <div class="stat-icon">
      <slot name="icon"></slot>
    </div>
    <div class="stat-content">
      <div class="stat-label">{{ label }}</div>
      <div class="stat-value">{{ value }}</div>
      <div class="stat-trend" :class="trendClass">
        <span>{{ trend }}</span>
        <i :class="trendIcon"></i>
      </div>
    </div>
  </div>
</template>

<script setup lang="ts">
interface Props {
  label: string
  value: string | number
  trend?: string
  trendType?: 'up' | 'down'
}
</script>
```

#### 4.1.2 ChartCard (图表卡片)
```vue
<template>
  <div class="chart-card">
    <div class="chart-header">
      <h3>{{ title }}</h3>
      <div class="chart-actions">
        <el-button icon="Refresh" @click="refresh" />
        <el-button icon="Download" @click="download" />
      </div>
    </div>
    <div class="chart-body">
      <div ref="chartRef" class="chart-container"></div>
    </div>
  </div>
</template>

<script setup lang="ts">
import * as echarts from 'echarts'
import { ref, onMounted, watch } from 'vue'

interface Props {
  title: string
  option: echarts.EChartsOption
}
</script>
```

#### 4.1.3 StatusBadge (状态徽章)
```vue
<template>
  <span class="status-badge" :class="`status-${status}`">
    <i class="status-dot"></i>
    <span>{{ text }}</span>
  </span>
</template>

<script setup lang="ts">
interface Props {
  status: 'online' | 'offline' | 'error' | 'unknown'
  text?: string
}
</script>

<style scoped>
.status-online { color: #67c23a; }
.status-offline { color: #f56c6c; }
.status-error { color: #e6a23c; }
.status-unknown { color: #909399; }
</style>
```

#### 4.1.4 DataTable (数据表格)
```vue
<template>
  <div class="data-table">
    <el-table
      :data="data"
      :loading="loading"
      @selection-change="handleSelectionChange"
    >
      <el-table-column type="selection" width="55" />
      <el-table-column
        v-for="column in columns"
        :key="column.prop"
        :prop="column.prop"
        :label="column.label"
        :width="column.width"
      >
        <template #default="{ row }">
          <slot :name="column.prop" :row="row">
            {{ row[column.prop] }}
          </slot>
        </template>
      </el-table-column>
      <el-table-column label="操作" width="180">
        <template #default="{ row }">
          <slot name="actions" :row="row"></slot>
        </template>
      </el-table-column>
    </el-table>
    <el-pagination
      v-model:current-page="currentPage"
      v-model:page-size="pageSize"
      :total="total"
      @current-change="handlePageChange"
    />
  </div>
</template>
```

### 4.2 业务组件

#### 4.2.1 DeviceCard (设备卡片)
```vue
<template>
  <div class="device-card" @click="handleClick">
    <div class="device-header">
      <StatusBadge :status="device.status" />
      <el-dropdown>
        <el-button icon="MoreFilled" />
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item @click="handleEdit">编辑</el-dropdown-item>
            <el-dropdown-item @click="handleDelete">删除</el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
    </div>
    <div class="device-body">
      <div class="device-icon">
        <i :class="deviceIcon"></i>
      </div>
      <h3>{{ device.name }}</h3>
      <p>{{ device.device_type }}</p>
      <div class="device-tags">
        <el-tag v-for="tag in device.labels" :key="tag">
          {{ tag }}
        </el-tag>
      </div>
    </div>
    <div class="device-footer">
      <div class="device-metric">
        <span>CPU</span>
        <el-progress :percentage="device.cpu_usage" />
      </div>
      <div class="device-metric">
        <span>内存</span>
        <el-progress :percentage="device.memory_usage" />
      </div>
    </div>
  </div>
</template>
```

#### 4.2.2 AlertTimeline (告警时间线)
```vue
<template>
  <div class="alert-timeline">
    <el-timeline>
      <el-timeline-item
        v-for="alert in alerts"
        :key="alert.id"
        :timestamp="formatTime(alert.triggered_at)"
        :color="getAlertColor(alert.severity)"
      >
        <div class="alert-item">
          <div class="alert-header">
            <span class="alert-severity">{{ alert.severity }}</span>
            <span class="alert-title">{{ alert.message }}</span>
          </div>
          <div class="alert-content">
            <p>设备: {{ alert.device_id }}</p>
            <p>指标: {{ alert.metric_name }}</p>
            <p>当前值: {{ alert.current_value }}</p>
          </div>
          <div class="alert-actions">
            <el-button size="small" @click="acknowledge(alert)">
              确认
            </el-button>
            <el-button size="small" @click="resolve(alert)">
              解决
            </el-button>
            <el-button size="small" @click="silence(alert)">
              静默
            </el-button>
          </div>
        </div>
      </el-timeline-item>
    </el-timeline>
  </div>
</template>
```

## 5. 状态管理

### 5.1 Store 结构

```typescript
// stores/index.ts
export const useUserStore = defineStore('user', {
  state: () => ({
    token: '',
    userInfo: null,
    permissions: []
  }),
  actions: {
    async login(username: string, password: string) {},
    async logout() {},
    async getUserInfo() {}
  }
})

export const useDeviceStore = defineStore('device', {
  state: () => ({
    devices: [],
    currentDevice: null,
    deviceGroups: []
  }),
  actions: {
    async fetchDevices(params) {},
    async createDevice(device) {},
    async updateDevice(id, device) {},
    async deleteDevice(id) {}
  }
})

export const useSentinelStore = defineStore('sentinel', {
  state: () => ({
    sentinels: [],
    currentSentinel: null
  }),
  actions: {
    async fetchSentinels() {},
    async getSentinelDetail(id) {}
  }
})

export const useAlertStore = defineStore('alert', {
  state: () => ({
    rules: [],
    events: [],
    unreadCount: 0
  }),
  actions: {
    async fetchRules() {},
    async fetchEvents() {},
    async acknowledgeEvent(id) {},
    async resolveEvent(id) {}
  }
})
```

## 6. API 接口封装

### 6.1 API 模块

```typescript
// api/device.ts
export const deviceApi = {
  // 获取设备列表
  getDevices: (params: DeviceQuery) => 
    request.get('/api/v1/devices', { params }),
  
  // 获取设备详情
  getDevice: (id: string) => 
    request.get(`/api/v1/devices/${id}`),
  
  // 创建设备
  createDevice: (data: DeviceCreate) => 
    request.post('/api/v1/devices', data),
  
  // 更新设备
  updateDevice: (id: string, data: DeviceUpdate) => 
    request.put(`/api/v1/devices/${id}`, data),
  
  // 删除设备
  deleteDevice: (id: string) => 
    request.delete(`/api/v1/devices/${id}`),
  
  // 测试连接
  testConnection: (id: string) => 
    request.post(`/api/v1/devices/${id}/test-connection`)
}

// api/sentinel.ts
export const sentinelApi = {
  getSentinels: (params) => request.get('/api/v1/sentinels', { params }),
  getSentinel: (id) => request.get(`/api/v1/sentinels/${id}`),
  controlSentinel: (id, action) => 
    request.post(`/api/v1/sentinels/${id}/control`, { action })
}

// api/alert.ts
export const alertApi = {
  getRules: (params) => request.get('/api/v1/alert-rules', { params }),
  createRule: (data) => request.post('/api/v1/alert-rules', data),
  updateRule: (id, data) => request.put(`/api/v1/alert-rules/${id}`, data),
  deleteRule: (id) => request.delete(`/api/v1/alert-rules/${id}`),
  
  getEvents: (params) => request.get('/api/v1/alert-events', { params }),
  acknowledgeEvent: (id) => 
    request.post(`/api/v1/alert-events/${id}/acknowledge`),
  resolveEvent: (id) => 
    request.post(`/api/v1/alert-events/${id}/resolve`),
  silenceEvent: (id, duration) => 
    request.post(`/api/v1/alert-events/${id}/silence`, { duration })
}
```

## 7. 实时通信

### 7.1 WebSocket 集成

```typescript
// utils/websocket.ts
class WebSocketClient {
  private ws: WebSocket | null = null
  private reconnectTimer: number | null = null
  private heartbeatTimer: number | null = null
  
  connect(url: string) {
    this.ws = new WebSocket(url)
    
    this.ws.onopen = () => {
      console.log('WebSocket connected')
      this.startHeartbeat()
    }
    
    this.ws.onmessage = (event) => {
      const data = JSON.parse(event.data)
      this.handleMessage(data)
    }
    
    this.ws.onclose = () => {
      console.log('WebSocket disconnected')
      this.reconnect()
    }
    
    this.ws.onerror = (error) => {
      console.error('WebSocket error:', error)
    }
  }
  
  private handleMessage(data: any) {
    switch (data.type) {
      case 'device_status_change':
        // 更新设备状态
        break
      case 'alert_triggered':
        // 显示新告警
        break
      case 'sentinel_heartbeat':
        // 更新 Sentinel 状态
        break
    }
  }
  
  private startHeartbeat() {
    this.heartbeatTimer = setInterval(() => {
      this.send({ type: 'ping' })
    }, 30000)
  }
  
  send(data: any) {
    if (this.ws && this.ws.readyState === WebSocket.OPEN) {
      this.ws.send(JSON.stringify(data))
    }
  }
  
  close() {
    if (this.ws) {
      this.ws.close()
    }
  }
}

export const wsClient = new WebSocketClient()
```

## 8. 响应式设计

### 8.1 断点定义

```scss
$breakpoints: (
  'xs': 0,
  'sm': 576px,
  'md': 768px,
  'lg': 992px,
  'xl': 1200px,
  'xxl': 1600px
);

// 移动端适配
@media (max-width: 768px) {
  .sidebar {
    position: fixed;
    left: -250px;
    transition: left 0.3s;
    
    &.active {
      left: 0;
    }
  }
  
  .main-content {
    margin-left: 0;
  }
  
  .stat-cards {
    grid-template-columns: 1fr;
  }
  
  .chart-grid {
    grid-template-columns: 1fr;
  }
}
```

## 9. 性能优化

### 9.1 优化策略

1. **懒加载**: 路由懒加载、组件懒加载
2. **虚拟滚动**: 大列表使用虚拟滚动
3. **防抖节流**: 搜索、窗口 resize 等事件
4. **缓存策略**: API 响应缓存、组件缓存
5. **代码分割**: 按需加载第三方库
6. **图片优化**: WebP 格式、懒加载
7. **打包优化**: Tree shaking、代码压缩

```typescript
// 路由懒加载
const routes = [
  {
    path: '/dashboard',
    component: () => import('@/views/Dashboard.vue')
  },
  {
    path: '/devices',
    component: () => import('@/views/Devices.vue')
  }
]

// 虚拟滚动
import { useVirtualList } from '@vueuse/core'

const { list, containerProps, wrapperProps } = useVirtualList(
  devices,
  { itemHeight: 60 }
)
```

## 10. 部署方案

### 10.1 Docker 部署

```dockerfile
# Dockerfile
FROM node:18-alpine AS builder

WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY --from=builder /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

### 10.2 Nginx 配置

```nginx
server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript;

    # SPA 路由
    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api/ {
        proxy_pass http://gravital-core:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket 代理
    location /ws/ {
        proxy_pass http://gravital-core:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
```

## 11. 开发规范

### 11.1 目录结构

```
web/
├── src/
│   ├── api/                 # API 接口
│   │   ├── device.ts
│   │   ├── sentinel.ts
│   │   ├── alert.ts
│   │   └── index.ts
│   ├── assets/              # 静态资源
│   │   ├── images/
│   │   ├── icons/
│   │   └── styles/
│   ├── components/          # 通用组件
│   │   ├── common/
│   │   │   ├── StatCard.vue
│   │   │   ├── ChartCard.vue
│   │   │   └── DataTable.vue
│   │   └── business/
│   │       ├── DeviceCard.vue
│   │       └── AlertTimeline.vue
│   ├── composables/         # 组合式函数
│   │   ├── useDevice.ts
│   │   ├── useAlert.ts
│   │   └── useWebSocket.ts
│   ├── layouts/             # 布局组件
│   │   ├── DefaultLayout.vue
│   │   └── BlankLayout.vue
│   ├── router/              # 路由配置
│   │   └── index.ts
│   ├── stores/              # 状态管理
│   │   ├── user.ts
│   │   ├── device.ts
│   │   └── alert.ts
│   ├── types/               # TypeScript 类型
│   │   ├── device.ts
│   │   ├── sentinel.ts
│   │   └── alert.ts
│   ├── utils/               # 工具函数
│   │   ├── request.ts
│   │   ├── websocket.ts
│   │   └── format.ts
│   ├── views/               # 页面组件
│   │   ├── Dashboard.vue
│   │   ├── Devices/
│   │   │   ├── List.vue
│   │   │   └── Detail.vue
│   │   ├── Sentinels/
│   │   ├── Tasks/
│   │   ├── Alerts/
│   │   └── Settings/
│   ├── App.vue
│   └── main.ts
├── public/
├── index.html
├── package.json
├── tsconfig.json
├── vite.config.ts
└── README.md
```

### 11.2 命名规范

- **组件**: PascalCase (DeviceCard.vue)
- **文件夹**: kebab-case (device-management/)
- **变量**: camelCase (deviceList)
- **常量**: UPPER_SNAKE_CASE (API_BASE_URL)
- **CSS 类**: kebab-case (device-card)

### 11.3 Git 提交规范

```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
perf: 性能优化
test: 测试相关
chore: 构建/工具链相关

示例:
feat(device): 添加设备批量导入功能
fix(alert): 修复告警通知不发送的问题
```

## 12. 下一步计划

### 12.1 Phase 1 (基础功能)
- [ ] 搭建项目框架
- [ ] 实现登录/认证
- [ ] 实现仪表盘
- [ ] 实现设备管理
- [ ] 实现 Sentinel 管理

### 12.2 Phase 2 (核心功能)
- [ ] 实现任务管理
- [ ] 实现告警管理
- [ ] 实现数据转发管理
- [ ] 实现实时通信

### 12.3 Phase 3 (增强功能)
- [ ] 实现数据可视化
- [ ] 实现移动端适配
- [ ] 实现国际化
- [ ] 性能优化

### 12.4 Phase 4 (高级功能)
- [ ] 实现自定义 Dashboard
- [ ] 实现插件市场
- [ ] 实现多租户
- [ ] 实现 AI 辅助

---

**设计版本**: v1.0  
**更新日期**: 2025-11-02  
**设计者**: AI Assistant

